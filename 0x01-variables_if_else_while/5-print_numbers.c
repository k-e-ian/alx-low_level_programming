#include <stdio.h>

/**
 * main - Entry mode
 *
 * variable assignment and type
 *
 * forloop function
 *
 * putchar function
 * Return: Always 0 (Success)
 */
int main(void)
{
	char num = '0';

	for (num = '0'; num <= '9'; num++)
		putchar(num);
			putchar('\n');
	return (0);
}
